import { colors } from "../constants/colors";
import { sizes } from "../constants/fonts";

export const theme = {
  colors,
  fonts: { sizes },
  spacing: {
    xs: 4,
    sm: 8,
    md: 16,
    lg: 24,
    xl: 32,
  },
};
